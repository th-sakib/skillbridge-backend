## Authentication flow

- User can be registered as Student, Tutor.
- A admin will be seeded by the server.
- But if anyone want to register as a admin they must provide a secret(which is defined by server & not be public for any general user) as `adminKey` property.
